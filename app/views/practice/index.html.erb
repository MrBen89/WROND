<h2>pick 5 kanjis to practice</h2>

<div class="kanji-boxes">
      <% @unlocked_kanji.each do |kanji| %>
        <span class="kanji-character" data-kanji-target="kanji" data-kanji="<%= kanji %>">
        <%= kanji %>
        </span>
    <% end %>
  </div>
</div>
